<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="view_employee_form" model="ir.ui.view">
        <field name="name">hr.employee.view.form.inherit.hr.zk.attendance</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='hr_settings']//field[@name='user_id']" position="after">
                <field name="device_id_num"/>
            </xpath>
        </field>
    </record>

    <record id="view_attendance_tree_inherit_zk" model="ir.ui.view">
        <field name="name">hr.attendance.tree.inherit.zk</field>
        <field name="model">hr.attendance</field>
        <field name="inherit_id" ref="hr_attendance.view_attendance_tree"/>
        <field name="arch" type="xml">
            <field name="worked_hours" position="before">
                <field name="status_ids" widget="many2many_tags" options="{'no_create': True}"/>
            </field>
        </field>
    </record>

    <record id="hr_attendance_view_search_inherit_zk" model="ir.ui.view">
        <field name="name">hr.attendance.view.search.inherit.zk</field>
        <field name="model">hr.attendance</field>
        <field name="inherit_id" ref="hr_attendance.hr_attendance_view_filter"/>
        <field name="arch" type="xml">
            <xpath expr="//search" position="inside">
                <separator/>
                <filter name="filter_missed_checkin" string="Missed Check-In"
                        domain="[('was_missing_checkin', '=', True)]"/>
                <filter name="filter_missed_checkout" string="Missed Check-Out"
                        domain="[('was_missing_checkout', '=', True)]"/>
            </xpath>
        </field>
    </record>
</odoo>